<%- include("header") %>
<!DOCTYPE html>
<html lang="en">
<%- include("navbar") %>
<br><br>
<div class="container">
    <h1><%=title%></h1>
    <% if(employees.length) {%>
        <p>‐‐ NameListing ‐‐</p>
        <br/><br>
        <a href="/new">Add New</a>
            <table class="tabletable‐hover">
                <thead class="thead‐dark">
                    <tr>
                        <th>First Name</th>
                        <th>Last Name</th>
                    </tr>
                </thead>
                <tbody>
                    <%employees.forEach(function(entry){%>
                    <tr>
                        <td><%=entry.firstName%></td>
                        <td><%=entry.lastName%></td>
                    </tr>
                </tbody>
                <%})%>
            </table>
            <br><br>
                <%}else{%> Noentries! <a href="/new">Add one!</a>
    <%}%>
</div>
<%- include("footer") %>
<%- include("scripts") %>
</body>
</html>